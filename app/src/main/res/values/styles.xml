<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/colorPrimary</item>
        <item name="itemBackground">@color/colorPrimaryDark</item>
        <item name="itemIconTint">@color/design_default_color_primary</item>
        <item name="itemTextColor">#FFFFFF</item>
        <item name="paddingStart">@color/design_default_color_primary</item>
        <item name="paddingEnd">@color/design_default_color_primary</item>
    </style>

    <style name="menu_text_style" parent="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Menu">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="style_btn" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_selector</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="transparent_button_style" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Add attributes here -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/tertiary_text_dark</item>
        <item name="editTextColor">@android:color/tertiary_text_dark</item>
        <item name="android:icon">@drawable/plus1</item>
    </style>

</resources>
